{
  "name": "live_status_feedback",
  "version": "1.0.0",
  "description": "hesel req",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "migrate": "sequelize-cli db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.23.0",
    "socket.io": "^4.5.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.4.1"
  }
}
